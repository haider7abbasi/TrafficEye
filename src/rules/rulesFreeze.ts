/**
 * Bump when you change spec ↔ Roboflow mapping, plate normalization, or challan PDF field keys
 * so downstream (Firestore, PDF) can detect stale payloads.
 */
export const TRAFFICEYE_RULES_FREEZE_VERSION = 1;
